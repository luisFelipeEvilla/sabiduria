<!DOCTYPE html>
<html lang="en">



<head>
  <%- include('../../partials/head'); %>
</head>

<body>

  <header>
    <%- include('../../partials/header'); %>
  </header>

  <main>
    <div class="container" style="padding-top: 20px; max-width: 500px;">
      <form action="../<%= plan.planestudio[0].id %>/agregarSemestre" method="POST"
        enctype="application/x-www-form-urlencoded">
        <div class="mb-3">

          <h5>Número de Semestre</h5>
          <select class="form-control" id ="numero" name = "numero"> 
            <% semestresDisponibles.forEach(semestre=> {%>
              <option value='<%= semestre %>'><%= semestre %></option>
            <% })%>
          </select>

          <div class="card" style="margin-top:20px">
            <div class="card-body">
              <h5 class="card-title" style="margin-top:20px">Seleccione las asignaturas</h2>

              <div class="form-group">
                <label style="margin-top:10px" for="nombre" class="form-label">Asignatura 1</label>
                <select class="form-control" id="id_asignatura_1" name="id_asignatura_1">
                  <% asignaturas.forEach(asignatura=> {%>
                    <option value='<%= asignatura.id %>'><%= asignatura.nombre %></option>
                  <% })%>
                </select>
              </div>
      
              <div class="form-group">
                <label style="margin-top:10px" for="nombre" class="form-label">Asignatura 2</label>
                <select class="form-control" id="id_asignatura_2" name="id_asignatura_2">                    
                  <% asignaturas.forEach(asignatura=> {%>
                    <option value='<%= asignatura.id %>'><%= asignatura.nombre %></option>
                  <% })%>
                </select>
              </div>
               
              <label style="margin-top:10px" for="nombre" class="form-label">Asignatura 3</label>
              <div class="form-group">
                <select class="form-control" id="id_asignatura_3" name="id_asignatura_3">
                  <% asignaturas.forEach(asignatura=> { %>
                    <option value='<%= asignatura.id %>'><%= asignatura.nombre %></option>
                  <% })%>
                </select>
              </div>
            </div>
            
          </div>
        </div>
        <button type="submit" class="btn btn-primary">Enviar</button>
      </form>
    </div>
  </main>
</body>

</html>